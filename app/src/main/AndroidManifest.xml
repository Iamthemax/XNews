<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.rgotechnologies.xnews">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:largeHeap="true"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyApp">
        <activity android:name=".xactivities.ContactActivity"></activity>
        <activity android:name=".xactivities.OptionsActivity" />
        <activity android:name=".xactivities.FavouritesActivity" />
        <activity android:name=".xactivities.AboutActivity" />
        <activity android:name=".xactivities.PostDetailsActivity" />
        <activity android:name=".xactivities.ViewPostsByCategoriesActivity" />
        <activity android:name=".xactivities.CategoriesActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".xactivities.Activity_Search">
            <meta-data
                android:name="android.app.searchable"
                android:resource="@xml/searchable" />
        </activity>
        <activity android:name=".xactivities.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>